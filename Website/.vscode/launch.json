{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Server and Launch Chrome",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/server.js",
            "cwd": "${workspaceFolder}",
            "runtimeExecutable": "node",
            "console": "integratedTerminal",
            "preLaunchTask": "open-chrome"
        },
    ]
}